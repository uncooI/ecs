@page
@model Roblox.Website.Pages.Auth.Captcha

@section MetaTags {
    <title>Captcha Challenge – Shklurb</title>
}

<style>
    @import url('https://fonts.googleapis.com/css?family=Segoe+UI:700,400|Tahoma:700,400');
    body { background: linear-gradient(135deg, #e2f0ff 0%, #7ad0ff 100%); font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;}
    .aero-logo { margin: 54px auto 20px auto; display: block; width: 220px; max-width: 76vw; filter: drop-shadow(0 8px 16px #69e2f6cc); border-radius: 28px;}
    .main-aero-card { background: linear-gradient(120deg, #f7fdff 60%, #8feeff30 100%); border-radius: 28px; box-shadow: 0 10px 60px #6fc6fc29, 0 2px 8px #7cdeff30; padding: 28px 26px 22px 26px; max-width: 370px; width: 96vw; text-align: center; margin: 24px auto 22px auto;}
    .main-aero-card h2 { font-size: 1.4em; color: #066bb8; margin-bottom: 10px; text-shadow: 0 2px 8px #e8fcff6b;}
    .main-aero-card p { color: #186b91; font-size: 1.02em; margin-bottom: 18px;}
    .cta-button { font-size: 1.10em; background: linear-gradient(90deg, #30d5fc 10%, #5ec5ff 100%); border: none; color: #fff; padding: 12px 34px; border-radius: 18px; font-weight: bold; box-shadow: 0 8px 26px #00bcf855; cursor: pointer; letter-spacing: 0.03em; text-shadow: 0 2px 8px #fff9;}
</style>

<img class="aero-logo" src="/UnsecuredContent/shklurb-logo.png" alt="Shklurb Logo">

<div class="main-aero-card">
    <h2>Captcha Challenge</h2>
    <p>Please complete the captcha below to continue.</p>
    <form method="post">
        <div class="h-captcha" data-sitekey="@Model.siteKey"></div>
        <button class="cta-button" type="submit" style="margin-top:18px;">Continue</button>
    </form>
</div>
<script src="https://hcaptcha.com/1/api.js" async defer></script>
